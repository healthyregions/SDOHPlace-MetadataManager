version: "3"
services:
    nginx:
        image: nginx
        build:
            context: ./nginx
            dockerfile: Dockerfile
        ports:
            - 8080:8080
        restart: always
    MetadataManager:
        depends_on:
            - nginx
        image: flask
        build: ./MetadataManager
        restart: always
        env_file: ./MetadataManager/.env

# docker-compose up -d --build
# docker-compose down
